<%= form_with(model: survey_answer, local: true) do |form| %>
  <% if survey_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey_answer.errors.count, "error") %> prohibited this survey_answer from being saved:</h2>

      <ul>
        <% survey_answer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :survey_id %>
    <%= form.text_field :survey_id %>
  </div>

  <div class="field">
    <%= form.label :answers %>
    <%= form.text_field :answers %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
